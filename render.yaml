services:
  - type: web
    name: mcf-wave
    runtime: node
    rootDir: apps/wave
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        generateValue: false
